apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shopbia_helm.fullname" . }}-test-connection"
  labels:
    {{- include "shopbia_helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shopbia_helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
